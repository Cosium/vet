version: '3.4'

services:
  gerrit:
    image: gerritcodereview/gerrit:2.15.rc2
    ports:
      - "8080"
    links:
      - ldap
    volumes:
     - ./git:/var/gerrit/git
     - ./etc:/var/gerrit/etc
     - ./logs:/var/gerrit/logs
  ldap:
    image: rroemhild/test-openldap